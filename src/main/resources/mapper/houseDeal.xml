<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.happyhouse.model.mapper.HouseDealMapper">
	<select id="selectByAptName" parameterType="String"  resultType="houseDealDto">
		select no, dong, aptName, dealAmount, buildYear, dealYear, dealMonth, type 
		from housedeal
		where AptName like CONCAT('%', #{AptName}, '%')
	</select>
	
	<select id="selectByDong" parameterType="String"  resultType="houseDealDto">
		select no, dong, aptName, dealAmount, buildYear, dealYear, dealMonth, type 
		from housedeal
		where dong like CONCAT('%', #{dong}, '%')
	</select>

</mapper>